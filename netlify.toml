[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Increase Node's max heap for builds to avoid "heap out of memory" on Netlify
  NODE_OPTIONS = "--max-old-space-size=4096"
